class Solution:
    def findMaxAverage(self, nums: list[int], k: int) -> float:
        i = 0
        win = sol = sum(nums[:k])

        for i in range(len(nums) - k):
            win = win - nums[i] + nums[i + k]
            sol = max(sol, win)

        return float(sol) / k
    